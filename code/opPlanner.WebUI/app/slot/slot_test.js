'use strict';

describe('myApp.slot module', function() {

  beforeEach(module('myApp.slot'));

  describe('slot controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var slotCtrl = $controller('slotCtrl');
      expect(slotCtrl).toBeDefined();
    }));

  });
});