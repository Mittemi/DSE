'use strict';

describe('myApp.listop module', function() {

  beforeEach(module('myApp.listop'));

  describe('listop controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var listopCtrl = $controller('listopCtrl');
      expect(listopCtrl).toBeDefined();
    }));

  });
});